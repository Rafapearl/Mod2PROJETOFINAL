<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">
  <title>Editar</title>
</head>

<body>

  <% if(message != "") {  %>
    <section id="message">
        <p><%= message %></p> <span id="close">&times;</span>
    </section>
<% }  %>

    <section class="area__editar">

      <form action="/editar/<%= artigo.id %>" method="POST">

        <div>
           <label for="titulo">Altere o título</label>
           <input type="text" name="nome" id="titulo" value="<%= artigo.titulo %>">
        </div>

        <div>
          <label for="nome">Altere seu nome</label>
          <input type="text" name="nome" id="nome" value="<%= artigo.nome %>">
        </div>

        <div>
          <label for="email">Altere o e-mail</label>
          <input type="text" name="email" id="email" value="<%= artigo.email %>">
        </div>

        <div>
          <label for="texto">Altere o artigo</label>
          <input type="text" name="texto" id="publi"value="<%= artigo.publi %>">
        </div>

          <button type="submit">Editar</button>
      </form>

    </section>
   </body>
</html>